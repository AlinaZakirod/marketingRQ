import React from "react";

const header = () => {
  return (
    // <header className="App-header">
    <header>
      <h1 className="App-title">SEARCH + COMPARE + BOOK</h1>
      <h3>Header.let's find your employees cool places</h3>
    </header>
  );
};

export default header;
